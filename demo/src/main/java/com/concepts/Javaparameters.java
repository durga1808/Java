package com.concepts;

public class Javaparameters
{
    public void myMethod1(String ename, int age, String city) {
        System.out.println(ename + " is " + age + " from " + city);//Constructors can also take parameters, which is used to initialize attributes.
      }
         
}